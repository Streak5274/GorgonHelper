# OS
Thumbs.db
.DS_Store

# Temporary / tool output
api-result.xml
scrape_training.py

# Game exports (auto-generated, no need to track)
Character_*.json
*_items_*Z.json
pg_landmarks.zip

.claude/
Original/

# pg_* files replaced by Json/ folder (CDN originals)
pg_*.json
RecycleBin/

# Python
__pycache__/
*.pyc

# Survey runtime data (config, saved state)
Survey/data/

# Icons downloaded from CDN — users pull these via the tool
icons/icon_*.png
icons/Map_*.png

# Wiki marked maps and calibration scripts (not used by the app)
Maps/marked/
Maps/calibrate_maps.py
Maps/calibration_from_wiki.json
Maps/map_calibration.json
